int main() {
	return 0;
}




// todo , need to make sure it works correctly
// this one breaks
int test1(int a, int b) {
	int c;
	a = 3;
	if(a < 5) {
		int d = a*5; // it's this, we added nullptr to phi functions so we can later eliminate them
		c = a / b;
		a = 0;
	}
	else {
		a = a * 2;
	}
	int e = a + c;
	while(a != 0) {
	}
	return a*b*c*e;
}


/*
this fails
int test() {
    while (true) {
        int x;
        x = readInt();
        if (x == 1)
					while(x == 1) {
            return 0;
					}
        else
            printString("jeszcze raz");
    }
}
but this is ok
int test() {
    while (true) {
        int x;
        x = readInt();
        if (x == 1)
					while(true) {
            return 0;
					}
        else
            printString("jeszcze raz");
    }
}

*/
